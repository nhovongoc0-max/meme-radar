# 对外发布检查清单

## 必须由项目所有者决定

- [x] 项目名称：Meme雷达开源版；作者：DeFi狙击手（X：@bi_9527zx）；
- [x] 选择 AGPL-3.0-only 并加入 `LICENSE`；
- [x] 使用 GitHub 私密漏洞报告入口；
- [ ] 核查 AVE、GoPlus、DexScreener 等上游服务的使用及商业条款；
- [x] 明确开源版与专业版承诺，不宣传保证盈利。

## 技术验收

- [x] 开源版与当前自用目录分离；
- [x] 发布副本不包含 API Key、运行状态和日志；
- [x] 应用只调用只读数据接口，不接入钱包交易、签名、swap 或下单；依赖包自身的其他能力不代表应用开放这些能力；
- [x] 提供 macOS 与 Windows 启动入口；
- [x] 已完成 macOS 安装和启动测试；
- [ ] v0.1.10 Windows 真机安装、语音播放及真实 API 连接需另行验收；历史版本的测试不计为本版真机测试；
- [x] 完成直接及传递依赖许可证复核；
- [x] 运行 `npm test`；
- [ ] 升级发布时先生成与 `package.json` 三段版本完全一致的 `MemeRadar-OpenSource-Windows-x64-<version>.zip`、`MemeRadar-OpenSource-macOS-<version>.zip` 和 `SHA256SUMS-<version>.txt`；
- [ ] 对最终待上传目录运行 `npm run release:audit -- --artifacts <发布资产目录>`；不允许用未打包的源码检查代替该步骤；
- [ ] 确认两个 ZIP 都包含且精确匹配当前发布源，包括用于旧版包迁移兼容测试的相关文件；审计脚本会同时阻断错误根目录、平台启动文件缺失、版本漂移、资产名不符和 SHA-256 不一致；
- [x] 使用已公开的独立开源仓库；
- [x] 项目所有者已授权同步更新及 Windows、macOS 双版本发布。

## v0.1.9 → v0.1.10 发布兼容与手动迁移验收

- [ ] 顶部“下载最新版”只打开官方 `https://github.com/nhovongoc0-max/meme-radar/releases/latest`；确认程序不会自动下载发布包、覆盖当前目录、退出、重启或安装新版本；
- [ ] 以官方 v0.1.9 macOS 包为基线运行 v0.1.10 包级迁移兼容测试，核对固定仓库资产名、版本、SHA-256、包结构及启动能力；该测试不代表界面会自动安装；
- [ ] 以官方 v0.1.9 Windows x64 包为基线运行同等的包级迁移兼容测试，并在真机验证新包可手动解压和启动；该测试不代表界面会自动安装；
- [ ] 两个平台在迁移兼容测试前后都核对本机 `state/`：AVE Key 可继续使用，轮询链、额度账本、收藏备注、风险排除和历史记录不丢失；验收记录不得打印 Key 原文；
- [ ] 两个平台均用可控失败包或健康检查失败验证测试工具的恢复能力：测试基线应恢复到 v0.1.9、可再次正常启动，且上述本机状态仍保留；这只是发布兼容验证，不是面向用户的自动回滚功能；
- [ ] 两个 ZIP 解压后与已批准的发布源文件清单及哈希逐项比对；只允许平台启动文件和 Windows 便携依赖等明确白名单差异，不得缺少运行源码、页面、更新器、脚本、`package.json` 或 `package-lock.json`；
- [ ] 完整包清单确认不含 API Key、`state/`、`logs/`、`.runtime/` 或本机个人资料；Windows 包中允许发布所需的固定依赖，但必须通过最终资产审计；
- [ ] GitHub Release 标签、`package.json`、`package-lock.json`、README 下载链接、两个 ZIP 文件名和 `SHA256SUMS-0.1.10.txt` 全部一致为 `0.1.10`；公布 Release 前不允许先将 README 下载链暴露为无法下载的死链；
